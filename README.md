# PHP `empty("0")` バリデーションデバッグラボ

PHPの `empty()` が文字列の `"0"` を空と判定するため、数量をゼロへ更新する正当な入力を必須エラーとして拒否する挙動を再現する。

| 項目 | 内容 |
|---|---|
| PHP | 8.3.6 CLI |
| 外部依存 | なし |
| 再現コマンド | `php tests/run.php` |

初期コミットでは、`"0"` を数量ゼロとして受け付けるテストが失敗する。修正後は空文字だけを未入力として扱い、同じテストを回帰テストとして成功させる。
